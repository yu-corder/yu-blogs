<%= form_with model: @bloggenre, local: true do |f| %>
    <div class = "form-group">
        <%= f.label :name, 'Name' %>
        <%= f.text_field :name, {class:"form-control"} %>
    </div>
    <p><%= f.submit "作成" %></p>
<% end %>